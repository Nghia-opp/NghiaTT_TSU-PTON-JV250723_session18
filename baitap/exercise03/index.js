const input = document.getElementById("input-title");
const btn = document.getElementById("btn-title");
const list = document.getElementById("list");

let todos = [];

function titleTodo () {
    const todosMaps = todos.map(function(value) {
        
    })
}